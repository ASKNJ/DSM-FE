import PropTypes from 'prop-types';


const FeedsPage = (props) => {
    return (
        <>
        
        </>
    );
};

FeedsPage.propTypes = {
    theme: PropTypes.string
}

export default FeedsPage;