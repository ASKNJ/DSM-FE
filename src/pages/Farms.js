import PropTypes from 'prop-types';


const FarmsPage = (props) => {
    return (
        <>
        
        </>
    );
};

FarmsPage.propTypes = {
    theme: PropTypes.string
}

export default FarmsPage;